# Third-Party Dependency Licenses

BOT_01 does not claim ownership of any third-party package it depends on.
Each dependency is governed by its own upstream license, checked as of the
versions pinned in `package-lock.json`. All are permissive licenses with
no copyleft or commercial-use restriction — none require this project's
own source to be open-sourced, and none prohibit commercial licensing of
BOT_01 itself.

## Runtime dependencies

| Package | License |
|---|---|
| `dotenv` | BSD-2-Clause |
| `pg` | MIT |
| `ws` | MIT |

## Development dependencies

| Package | License |
|---|---|
| `typescript` | Apache-2.0 |
| `ts-node` | MIT |
| `@types/node` | MIT |
| `@types/pg` | MIT |
| `@types/ws` | MIT |

This table was generated by inspecting each package's own `package.json`
`license` field in `node_modules` at the time of writing. If dependencies
are added or upgraded later, re-check this table — it is not automatically
kept in sync.
